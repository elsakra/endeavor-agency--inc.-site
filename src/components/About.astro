---
const stats = [
  { number: 25, label: "Years of Excellence" },
  { number: 500, label: "Executives Placed" },
  { number: 95, label: "Client Retention Rate", suffix: "%" },
  { number: 30, label: "Industries Served", suffix: "+" }
];
---

<section id="about" class="py-24 md:py-32 bg-cream-50">
  <div class="max-w-7xl mx-auto px-6 md:px-8">
    <div class="grid lg:grid-cols-2 gap-16 items-center">
      <div class="space-y-8">
        <p class="text-sm uppercase tracking-[0.2em] text-amber-600 font-medium animate-on-scroll">
          ABOUT ENDEAVOR
        </p>
        <h2 class="text-5xl md:text-6xl font-black tracking-tighter leading-[0.9] animate-on-scroll">
          Trusted by Leaders.<br/>Driven by Excellence.
        </h2>
        <div class="space-y-6 text-lg text-stone-600 animate-on-scroll">
          <p>
            For over two decades, Endeavor Agency has been the trusted partner for organizations seeking transformational leadership. We combine deep industry expertise with an unwavering commitment to excellence.
          </p>
          <p>
            Our boutique approach ensures personalized attention while our global network delivers world-class talent. We don't just fill positions—we forge partnerships that drive lasting success.
          </p>
        </div>
        <div class="pt-8 animate-on-scroll">
          <a href="#contact" class="inline-flex items-center gap-2 text-lg font-semibold text-amber-600 hover:text-amber-700 transition-colors duration-300">
            Learn more about our approach
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </a>
        </div>
      </div>
      
      <div class="relative">
        <!-- Background image -->
        <div class="absolute inset-0 -z-10">
          <img 
            src="https://images.unsplash.com/photo-1556761175-5973dc0f32e7?q=80&w=2532&auto=format&fit=crop" 
            alt="Modern office building"
            class="w-full h-full object-cover rounded-2xl opacity-10"
          />
        </div>
        
        <div class="grid grid-cols-2 gap-6">
          {stats.map((stat, index) => (
            <div class="bg-white rounded-2xl p-8 text-center hover:shadow-xl hover:-translate-y-1 transition-all duration-300 animate-on-scroll">
              <div class="text-4xl md:text-5xl font-black text-amber-600 mb-2">
                <span class="counter" data-count={stat.number}>0</span>{stat.suffix || ""}
              </div>
              <p class="text-sm text-stone-600 font-medium">{stat.label}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>